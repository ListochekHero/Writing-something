#include <vector>
#include <cstdint>
#include <math.h>
using namespace std;
std::vector<uint64_t> powers_of_two(int n) {
  int i;
vector<uint64_t> n_;
  for(i=0;i<=n;++i){
    n_.push_back(pow(2,i));
  }
vector<uint64_t> *a = new vector<uint64_t>(n_);
  return *a;
}
